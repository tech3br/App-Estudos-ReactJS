import React from 'react';

// Definindo variaveis
const incorp = 'Incorporando';
const expressoes = 'Expressoes';

// Incorporando expressoes
const element = <h1>{`${incorp} ${expressoes}`}</h1>;

export default () => element;
